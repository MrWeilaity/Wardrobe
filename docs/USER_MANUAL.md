# 用户操作手册 (User Manual)
# Smart Wardrobe User Manual

**项目名称 (Project Name)**: 智衣橱 - Smart Wardrobe  
**文档版本 (Version)**: V2.0  
**编制日期 (Date)**: 2025-12-15  
**文档类型**: 用户操作手册  
**适用对象**: 系统管理员、普通用户、运维人员  

---

## 📋 目录 (Table of Contents)

1. [系统简介](#1-系统简介)
2. [环境准备与部署](#2-环境准备与部署)
3. [用户操作指南](#3-用户操作指南)
4. [管理员操作指南](#4-管理员操作指南)
5. [常见问题FAQ](#5-常见问题faq)
6. [故障排查](#6-故障排查)
7. [附录](#7-附录)

---

## 1. 系统简介

### 1.1 系统概述

智衣橱是一个现代化的个人衣橱管理系统，帮助用户数字化管理衣物、创建穿搭方案、规划旅行行李。系统采用前后端分离架构，支持Web浏览器访问。

### 1.2 功能概览

| 功能模块 | 主要功能 |
|---------|---------|
| 🏠 **我的衣橱** | 衣物录入、分类筛选、搜索查询、详情查看 |
| 💃 **穿搭方案** | 创建穿搭、衣物搭配、场合分类 |
| ✈️ **旅行助手** | 创建旅行、打包清单、行李管理 |
| 📊 **数据中心** | 统计分析、数据可视化（管理员） |

### 1.3 系统访问信息

**访问地址**:
- 🌐 **前端界面**: http://localhost (Docker部署) 或 http://localhost:5173 (开发模式)
- 🔧 **后端API**: http://localhost:8080/api
- 📚 **API文档**: http://localhost:8080/swagger-ui.html (如已配置)

**默认账号**:
- **管理员**: 用户名 `admin` / 密码 `admin123`
- **测试用户**: 用户名 `zhangsan` / 密码 `password123`

---

## 2. 环境准备与部署

### 2.1 系统要求

#### 硬件要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|---------|---------|
| CPU | 2核 | 4核+ |
| 内存 | 4GB | 8GB+ |
| 磁盘 | 20GB | 50GB+ |
| 网络 | 10Mbps | 100Mbps+ |

#### 软件要求

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| Docker | 20.10+ | 容器运行 |
| Docker Compose | 2.0+ | 多容器编排 |
| Git | 2.0+ | 代码克隆 |
| 浏览器 | Chrome 90+, Firefox 88+, Safari 14+ | 访问界面 |

**或者手动部署需要**:
- Java JDK 11+
- Maven 3.6+
- Node.js 16+
- MySQL 5.7+

### 2.2 部署方式一：Docker一键部署（推荐）

#### 2.2.1 快速启动脚本

创建或使用项目提供的 `quick-start.sh` 脚本：

```bash
#!/bin/bash
# 智衣橱一键启动脚本
# 用途: 快速启动所有服务 (MySQL + Backend + Frontend + Nginx)

set -e

echo "========================================="
echo "  智衣橱系统 - 一键启动脚本"
echo "========================================="
echo ""

# 检查Docker是否安装
if ! command -v docker &> /dev/null; then
    echo "❌ 错误: 未检测到Docker，请先安装Docker"
    echo "   访问: https://docs.docker.com/get-docker/"
    exit 1
fi

# 检查Docker Compose是否安装
if ! command -v docker compose &> /dev/null; then
    echo "❌ 错误: 未检测到Docker Compose"
    echo "   请安装Docker Compose v2.0+"
    exit 1
fi

echo "✅ Docker环境检测通过"
echo ""

# 停止旧容器
echo "🔄 停止旧容器..."
docker compose down 2>/dev/null || true

# 清理旧镜像（可选）
read -p "是否清理旧镜像？(y/N): " -n 1 -r
echo
if [[ $REPLY =~ ^[Yy]$ ]]; then
    echo "🧹 清理旧镜像..."
    docker compose down --rmi all --volumes
fi

# 构建并启动服务
echo "🏗️  构建Docker镜像..."
docker compose build --no-cache

echo "🚀 启动所有服务..."
docker compose up -d

# 等待服务启动
echo ""
echo "⏳ 等待服务启动完成..."
sleep 10

# 检查服务状态
echo ""
echo "📊 服务状态检查:"
docker compose ps

# 检查MySQL健康状态
echo ""
echo "🔍 检查MySQL数据库..."
for i in {1..30}; do
    if docker compose exec -T mysql mysqladmin ping -h localhost -u root -p\${MYSQL_ROOT_PASSWORD} --silent 2>/dev/null; then
        echo "✅ MySQL数据库就绪"
        break
    fi
    if [ $i -eq 30 ]; then
        echo "❌ MySQL启动超时"
        exit 1
    fi
    echo "   等待MySQL启动... ($i/30)"
    sleep 2
done

# 检查后端服务
echo ""
echo "🔍 检查后端服务..."
for i in {1..30}; do
    if curl -f http://localhost:8080/api/health >/dev/null 2>&1; then
        echo "✅ 后端服务就绪"
        break
    fi
    if [ $i -eq 30 ]; then
        echo "⚠️  后端服务可能未完全启动，请检查日志"
    fi
    sleep 2
done

# 显示访问信息
echo ""
echo "========================================="
echo "  🎉 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址:"
echo "   前端界面: http://localhost"
echo "   后端API:  http://localhost:8080/api"
echo "   MySQL:    localhost:3306"
echo ""
echo "🔑 默认账号:"
echo "   管理员: admin / admin123"
echo "   用户:   zhangsan / password123"
echo ""
echo "📝 常用命令:"
echo "   查看日志: docker compose logs -f"
echo "   停止服务: docker compose down"
echo "   重启服务: docker compose restart"
echo ""
echo "========================================="
```

**使用方法**:
```bash
# 1. 赋予执行权限
chmod +x quick-start.sh

# 2. 执行脚本
./quick-start.sh
```

#### 2.2.2 Docker Compose配置文件

确保项目根目录有 `docker-compose.yml` 文件：

```yaml
version: '3.8'

services:
  # MySQL数据库服务
  mysql:
    image: mysql:5.7
    container_name: wardrobe-mysql
    restart: always
    environment:
      MYSQL_DATABASE: wardrobe
      MYSQL_USER: wardrobe_user
      MYSQL_PASSWORD: wardrobe_pass
      MYSQL_ROOT_PASSWORD: root_pass
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/src/main/resources/schema.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - wardrobe-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_pass"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    command: 
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=+08:00

  # Spring Boot后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wardrobe-backend
    restart: always
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/wardrobe?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
      SPRING_DATASOURCE_USERNAME: wardrobe_user
      SPRING_DATASOURCE_PASSWORD: wardrobe_pass
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SERVER_PORT: 8080
      JWT_SECRET: SmartWardrobeSecretKey2025ForProductionUse
      TZ: Asia/Shanghai
    ports:
      - "8080:8080"
    volumes:
      - ./backend/uploads:/app/uploads
      - backend_logs:/app/logs
    networks:
      - wardrobe-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Vue.js前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8080
    container_name: wardrobe-frontend
    restart: always
    depends_on:
      - backend
    networks:
      - wardrobe-network

  # Nginx反向代理
  nginx:
    image: nginx:alpine
    container_name: wardrobe-nginx
    restart: always
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./backend/uploads:/usr/share/nginx/html/uploads:ro
    networks:
      - wardrobe-network

volumes:
  mysql_data:
    driver: local
  backend_logs:
    driver: local

networks:
  wardrobe-network:
    driver: bridge
```

#### 2.2.3 后端Dockerfile

在 `backend/` 目录创建 `Dockerfile`:

```dockerfile
# 第一阶段: 构建
FROM maven:3.8-openjdk-11-slim AS build

WORKDIR /app

# 复制pom.xml并下载依赖
COPY pom.xml .
RUN mvn dependency:go-offline -B

# 复制源代码并构建
COPY src ./src
RUN mvn clean package -DskipTests -B

# 第二阶段: 运行
FROM openjdk:11-jre-slim

WORKDIR /app

# 安装curl用于健康检查
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 从构建阶段复制jar包
COPY --from=build /app/target/*.jar app.jar

# 创建上传目录
RUN mkdir -p /app/uploads

# 暴露端口
EXPOSE 8080

# 启动应用
ENTRYPOINT ["java", "-jar", "-Dspring.profiles.active=prod", "app.jar"]
```

#### 2.2.4 前端Dockerfile

在 `frontend/` 目录创建 `Dockerfile`:

```dockerfile
# 第一阶段: 构建
FROM node:16-alpine AS build

WORKDIR /app

# 复制package.json
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY . .

# 构建生产版本
ARG VITE_API_BASE_URL
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL
RUN npm run build

# 第二阶段: 运行
FROM nginx:alpine

# 复制构建产物
COPY --from=build /app/dist /usr/share/nginx/html

# 复制nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

# 启动nginx
CMD ["nginx", "-g", "daemon off;"]
```

前端nginx配置 `frontend/nginx.conf`:

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 开启gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

#### 2.2.5 Nginx主配置

项目根目录 `nginx.conf`:

```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;
    gzip on;

    # 上传大小限制
    client_max_body_size 10M;

    upstream frontend {
        server frontend:80;
    }

    upstream backend {
        server backend:8080;
    }

    server {
        listen 80;
        server_name localhost;

        # 前端页面
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 后端API
        location /api/ {
            proxy_pass http://backend/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # CORS配置
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
            
            if ($request_method = 'OPTIONS') {
                return 204;
            }
        }

        # 上传文件访问
        location /uploads/ {
            alias /usr/share/nginx/html/uploads/;
            autoindex off;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

#### 2.2.6 部署步骤详解

```bash
# 1. 克隆项目
git clone https://github.com/MrWeilaity/Wardrobe.git
cd Wardrobe

# 2. 配置环境变量（可选）
cp .env.example .env
# 编辑.env文件，修改数据库密码等配置

# 3. 构建并启动
docker compose build
docker compose up -d

# 4. 查看服务状态
docker compose ps

# 5. 查看日志
docker compose logs -f

# 6. 测试访问
curl http://localhost
curl http://localhost:8080/api/health

# 7. 停止服务
docker compose down

# 8. 完全清理（包括数据卷）
docker compose down -v
```

### 2.3 部署方式二：手动部署

#### 2.3.1 安装MySQL

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install mysql-server-5.7

# 启动MySQL
sudo systemctl start mysql
sudo systemctl enable mysql

# 创建数据库
mysql -u root -p
CREATE DATABASE wardrobe CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'wardrobe_user'@'localhost' IDENTIFIED BY 'wardrobe_pass';
GRANT ALL PRIVILEGES ON wardrobe.* TO 'wardrobe_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 导入初始化脚本（如有）
mysql -u wardrobe_user -p wardrobe < backend/src/main/resources/schema.sql
```

#### 2.3.2 部署后端

```bash
# 1. 进入后端目录
cd backend

# 2. 配置数据库连接
# 编辑 src/main/resources/application.yml
# 修改数据库URL、用户名、密码

# 3. 构建项目
mvn clean package -DskipTests

# 4. 运行
java -jar target/wardrobe-0.0.1-SNAPSHOT.jar

# 或者使用Maven运行
mvn spring-boot:run

# 后台运行
nohup java -jar target/wardrobe-0.0.1-SNAPSHOT.jar > backend.log 2>&1 &
```

#### 2.3.3 部署前端

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 配置API地址
# 编辑 .env 文件
echo "VITE_API_BASE_URL=http://localhost:8080" > .env.production

# 4. 构建生产版本
npm run build

# 5. 使用Nginx部署
sudo cp -r dist/* /var/www/html/

# 或使用Node.js服务器
npm install -g serve
serve -s dist -l 5173
```

#### 2.3.4 配置Nginx（手动部署）

```bash
# 创建配置文件
sudo nano /etc/nginx/sites-available/wardrobe

# 内容如下:
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /uploads/ {
        alias /path/to/backend/uploads/;
    }
}

# 启用站点
sudo ln -s /etc/nginx/sites-available/wardrobe /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 2.4 环境变量配置

创建 `.env` 文件（Docker部署）:

```bash
# 数据库配置
MYSQL_DATABASE=wardrobe
MYSQL_USER=wardrobe_user
MYSQL_PASSWORD=your_secure_password_here
MYSQL_ROOT_PASSWORD=your_root_password_here

# JWT配置
JWT_SECRET=your_jwt_secret_key_here_at_least_32_characters

# 应用配置
SERVER_PORT=8080
UPLOAD_DIR=/app/uploads

# 前端配置
VITE_API_BASE_URL=http://localhost:8080
```

---

## 3. 用户操作指南

### 3.1 注册与登录

#### 3.1.1 用户注册

```
操作步骤:
┌─────────────────────────────────────┐
│  1. 访问系统首页                    │
│     http://localhost                │
│                                     │
│  2. 点击"注册"按钮                  │
│                                     │
│  3. 填写注册信息:                   │
│     ┌─────────────────────┐        │
│     │ 用户名: ________    │        │
│     │ 密码:   ********    │        │
│     │ 确认密码: ******    │        │
│     │ 邮箱:   ________    │        │
│     │ 手机号: ________    │        │
│     └─────────────────────┘        │
│                                     │
│  4. 点击"注册"完成                  │
│                                     │
│  5. 跳转到登录页面                  │
└─────────────────────────────────────┘
```

**界面示意图**:
```
┌────────────────────────────────────────┐
│          智衣橱 - 用户注册             │
├────────────────────────────────────────┤
│                                        │
│  用户名: [___________________]         │
│                                        │
│  密码:   [___________________]         │
│                                        │
│  确认密码:[___________________]         │
│                                        │
│  邮箱:   [___________________]         │
│                                        │
│  手机号: [___________________]         │
│                                        │
│  [ 我已阅读并同意用户协议 ]            │
│                                        │
│        [  注册  ]  [  返回  ]          │
│                                        │
│  已有账号？ [立即登录]                 │
└────────────────────────────────────────┘
```

#### 3.1.2 用户登录

```
操作步骤:
┌─────────────────────────────────────┐
│  1. 访问登录页面                    │
│                                     │
│  2. 输入用户名和密码                │
│     ┌─────────────────────┐        │
│     │ 用户名: admin       │        │
│     │ 密码:   ********    │        │
│     └─────────────────────┘        │
│                                     │
│  3. 点击"登录"                      │
│                                     │
│  4. 系统验证身份                    │
│                                     │
│  5. 登录成功，跳转到主页            │
└─────────────────────────────────────┘
```

**界面示意图**:
```
┌────────────────────────────────────────┐
│          智衣橱 - 用户登录             │
├────────────────────────────────────────┤
│                                        │
│                [LOGO]                  │
│                                        │
│  用户名: [___________________]         │
│                                        │
│  密码:   [___________________]         │
│                                        │
│  [ ] 记住密码    [忘记密码？]         │
│                                        │
│           [  登 录  ]                  │
│                                        │
│  还没有账号？ [立即注册]               │
└────────────────────────────────────────┘
```

### 3.2 我的衣橱

#### 3.2.1 添加衣物

```
操作流程图:
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│ 点击   │    │ 填写   │    │ 上传   │    │ 保存   │
│ 添加   │───>│ 信息   │───>│ 图片   │───>│ 完成   │
└────────┘    └────────┘    └────────┘    └────────┘
```

**详细步骤**:

1. **进入我的衣橱页面**
   - 点击顶部导航栏"我的衣橱"

2. **点击添加衣物按钮**
   - 页面右上角"+ 添加衣物"按钮

3. **填写衣物信息**
   ```
   ┌───────────────────────────────────┐
   │ 添加衣物                          │
   ├───────────────────────────────────┤
   │ * 衣物名称: [____________]        │
   │                                   │
   │ * 类别:     [▼选择类别]           │
   │   选项: 上衣/裤装/裙装/外套/      │
   │         鞋履/配饰                 │
   │                                   │
   │   颜色:     [____________]        │
   │                                   │
   │   品牌:     [____________]        │
   │                                   │
   │   价格:     [____________] 元     │
   │                                   │
   │   尺寸:     [▼选择]               │
   │   选项: XS/S/M/L/XL/XXL/均码      │
   │                                   │
   │   季节:     [▼选择]               │
   │   选项: 春/夏/秋/冬/四季          │
   │                                   │
   │   材质:     [____________]        │
   │                                   │
   │   购买日期: [____年__月__日]      │
   │                                   │
   │   备注:                           │
   │   [_________________________]     │
   │   [_________________________]     │
   │                                   │
   │ * 上传图片: [选择文件] 未选择     │
   │   [图片预览区域]                  │
   │                                   │
   │   [ 取消 ]  [ 保存 ]              │
   └───────────────────────────────────┘
   ```

4. **上传图片**
   - 点击"选择文件"按钮
   - 从本地选择图片（支持JPG、PNG格式）
   - 图片大小建议不超过5MB
   - 查看预览效果

5. **提交保存**
   - 点击"保存"按钮
   - 等待上传和保存完成
   - 看到成功提示后，衣物会出现在列表中

#### 3.2.2 查看衣物列表

```
界面布局:
┌────────────────────────────────────────────────┐
│ 我的衣橱                          + 添加衣物   │
├────────────────────────────────────────────────┤
│ 筛选: [全部▼] [上衣] [裤装] [裙装] [外套]...  │
│ 季节: [全部▼] [春] [夏] [秋] [冬]             │
│ 搜索: [_____________] [🔍]                     │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐      │
│  │[图片]│  │[图片]│  │[图片]│  │[图片]│      │
│  │白色  │  │蓝色  │  │黑色  │  │运动  │      │
│  │T恤  │  │牛仔  │  │连衣  │  │鞋   │      │
│  │¥99  │  │裤   │  │裙   │  │¥299 │      │
│  └──────┘  │¥159 │  │¥299 │  └──────┘      │
│            └──────┘  └──────┘                 │
│                                                │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐      │
│  │[图片]│  │[图片]│  │[图片]│  │[图片]│      │
│  │...   │  │...   │  │...   │  │...   │      │
│  └──────┘  └──────┘  └──────┘  └──────┘      │
│                                                │
│          [上一页]  1  2  3  [下一页]          │
└────────────────────────────────────────────────┘
```

**操作说明**:

1. **筛选衣物**
   - 点击类别标签快速筛选
   - 选择季节过滤衣物
   - 多个条件可组合使用

2. **搜索衣物**
   - 在搜索框输入关键词
   - 支持按名称、品牌、颜色搜索
   - 实时显示搜索结果

3. **查看详情**
   - 点击衣物卡片
   - 查看完整信息和大图
   - 可以编辑或删除

#### 3.2.3 编辑衣物

```
操作步骤:
1. 在衣物列表中找到要编辑的衣物
2. 点击衣物卡片或"编辑"按钮
3. 修改信息（与添加界面相同）
4. 点击"保存"更新信息
```

#### 3.2.4 删除衣物

```
操作步骤:
1. 找到要删除的衣物
2. 点击"删除"按钮
3. 确认删除操作
   ┌─────────────────────┐
   │  确认删除？         │
   │                     │
   │  确定要删除这件衣物 │
   │  吗？此操作不可恢复 │
   │                     │
   │  [ 取消 ] [ 确定 ]  │
   └─────────────────────┘
4. 删除成功
```

### 3.3 穿搭方案

#### 3.3.1 创建穿搭

```
创建流程:
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ 填写    │    │ 选择    │    │ 预览    │    │ 保存    │
│ 基本信息│───>│ 衣物    │───>│ 确认    │───>│ 完成    │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
```

**详细操作**:

1. **进入穿搭管理页面**
   - 点击导航栏"穿搭方案"

2. **创建新穿搭**
   ```
   ┌────────────────────────────────────┐
   │ 创建穿搭方案                       │
   ├────────────────────────────────────┤
   │                                    │
   │ 穿搭名称: [__________________]     │
   │                                    │
   │ 适用场合: [▼选择场合]              │
   │   □ 日常  □ 工作  □ 约会          │
   │   □ 运动  □ 正式  □ 休闲          │
   │                                    │
   │ 备注说明:                          │
   │ [____________________________]     │
   │ [____________________________]     │
   │                                    │
   │ 选择衣物: [ 从衣橱选择 ]           │
   │                                    │
   │ 已选衣物:                          │
   │ ┌────┐ ┌────┐ ┌────┐ ┌────┐       │
   │ │上衣│ │裤子│ │鞋子│ │包包│       │
   │ │ ×  │ │ ×  │ │ ×  │ │ ×  │       │
   │ └────┘ └────┘ └────┘ └────┘       │
   │                                    │
   │    [ 取消 ]  [ 保存穿搭 ]          │
   └────────────────────────────────────┘
   ```

3. **选择衣物**
   - 点击"从衣橱选择"
   - 弹出衣橱衣物列表
   - 多选需要的衣物（勾选）
   - 确认选择

4. **保存穿搭**
   - 填写完整信息
   - 至少选择1件衣物
   - 点击"保存穿搭"

#### 3.3.2 查看穿搭列表

```
界面布局:
┌─────────────────────────────────────────┐
│ 穿搭方案                   + 创建穿搭   │
├─────────────────────────────────────────┤
│ 场合: [全部▼] [日常] [工作] [约会]...  │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────┐  ┌──────────────┐    │
│  │  夏日约会装  │  │  商务休闲    │    │
│  │ [衣物组合图] │  │ [衣物组合图] │    │
│  │ 场合: 约会   │  │ 场合: 工作   │    │
│  │ 4件衣物      │  │ 3件衣物      │    │
│  │ 2025-12-10   │  │ 2025-12-08   │    │
│  │ [查看][编辑] │  │ [查看][编辑] │    │
│  └──────────────┘  └──────────────┘    │
│                                         │
│  ┌──────────────┐  ┌──────────────┐    │
│  │  运动套装    │  │  正式晚宴    │    │
│  │ [衣物组合图] │  │ [衣物组合图] │    │
│  │ ...          │  │ ...          │    │
│  └──────────────┘  └──────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

### 3.4 旅行助手

#### 3.4.1 创建旅行计划

```
创建流程:
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ 填写    │    │ 添加    │    │ 管理    │    │ 完成    │
│ 旅行信息│───>│ 行李    │───>│ 清单    │───>│ 保存    │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
```

**详细步骤**:

1. **填写旅行信息**
   ```
   ┌────────────────────────────────────┐
   │ 创建旅行计划                       │
   ├────────────────────────────────────┤
   │                                    │
   │ 旅行名称: [三亚旅行___________]    │
   │                                    │
   │ 目的地:   [三亚_______________]    │
   │                                    │
   │ 出发日期: [2025-01-15] 📅          │
   │                                    │
   │ 返程日期: [2025-01-20] 📅          │
   │           (共6天5晚)               │
   │                                    │
   │ 旅行类型: ○ 度假休闲               │
   │           ○ 商务出差               │
   │           ○ 探险运动               │
   │                                    │
   │ 备注:                              │
   │ [_____________________________]    │
   │                                    │
   │    [ 取消 ]  [ 下一步 ]            │
   └────────────────────────────────────┘
   ```

2. **添加行李衣物**
   ```
   ┌────────────────────────────────────┐
   │ 三亚旅行 - 打包清单                │
   ├────────────────────────────────────┤
   │ 行李统计:                          │
   │ 上衣: 3件 | 裤装: 2件 | 鞋履: 2双 │
   │ 配饰: 1件 | 总计: 8件              │
   │ 完成度: [████████░░] 80%           │
   ├────────────────────────────────────┤
   │                                    │
   │ [ 从衣橱添加 ]  [ 智能推荐 ]       │
   │                                    │
   │ 已添加衣物:                        │
   │                                    │
   │ 上衣 (3):                          │
   │ ┌────┐ ┌────┐ ┌────┐              │
   │ │T恤 │ │衬衫│ │防晒│              │
   │ │白色│ │蓝色│ │衣  │              │
   │ │ ×  │ │ ×  │ │ ×  │              │
   │ └────┘ └────┘ └────┘              │
   │                                    │
   │ 裤装 (2):                          │
   │ ┌────┐ ┌────┐                     │
   │ │短裤│ │裙子│                     │
   │ │卡其│ │碎花│                     │
   │ │ ×  │ │ ×  │                     │
   │ └────┘ └────┘                     │
   │                                    │
   │    [ 上一步 ]  [ 完成 ]            │
   └────────────────────────────────────┘
   ```

3. **查看打包清单**
   - 按类别分组显示
   - 查看统计信息
   - 可继续添加或删除

#### 3.4.2 管理旅行清单

```
操作说明:

1. 编辑旅行信息
   - 点击旅行卡片
   - 修改日期、目的地等
   - 保存更新

2. 添加/移除衣物
   - 进入清单详情
   - 点击"添加"或衣物的"×"
   - 实时更新统计

3. 删除旅行计划
   - 点击"删除"按钮
   - 确认删除
```

### 3.5 个人设置

```
设置页面布局:
┌────────────────────────────────────┐
│ 个人设置                           │
├────────────────────────────────────┤
│                                    │
│ 基本信息                           │
│ 用户名: admin (不可修改)           │
│ 邮箱:   [__________________] 修改  │
│ 手机号: [__________________] 修改  │
│                                    │
│ 头像:   [当前头像]                 │
│         [ 上传新头像 ]             │
│                                    │
│ 修改密码                           │
│ 当前密码: [__________________]     │
│ 新密码:   [__________________]     │
│ 确认密码: [__________________]     │
│           [ 修改密码 ]             │
│                                    │
│ 通知设置                           │
│ □ 接收系统通知                     │
│ □ 接收邮件提醒                     │
│                                    │
│ [ 保存设置 ]  [ 退出登录 ]         │
└────────────────────────────────────┘
```

---

## 4. 管理员操作指南

### 4.1 管理员登录

使用管理员账号登录:
- 用户名: `admin`
- 密码: `admin123`

登录后可访问管理员控制面板。

### 4.2 系统统计

```
管理员Dashboard:
┌──────────────────────────────────────────┐
│ 系统统计                                 │
├──────────────────────────────────────────┤
│                                          │
│  用户统计                                │
│  ┌─────────┬─────────┬─────────┐        │
│  │ 总用户  │ 活跃用户│ 新增用户│        │
│  │  1,234  │   856   │   45    │        │
│  └─────────┴─────────┴─────────┘        │
│                                          │
│  数据统计                                │
│  ┌─────────┬─────────┬─────────┐        │
│  │ 总衣物  │ 总穿搭  │ 总旅行  │        │
│  │ 15,678  │  2,345  │   567   │        │
│  └─────────┴─────────┴─────────┘        │
│                                          │
│  [用户增长趋势图表]                      │
│  ████████████████░░░░                    │
│                                          │
│  [衣物类别分布图表]                      │
│  ●●●●●● 上衣 40%                        │
│  ●●●●   裤装 25%                        │
│  ●●●    裙装 15%                        │
│  ●●     其他 20%                        │
│                                          │
└──────────────────────────────────────────┘
```

### 4.3 用户管理

```
用户列表页面:
┌──────────────────────────────────────────────────┐
│ 用户管理                          [刷新] [导出]  │
├──────────────────────────────────────────────────┤
│ 搜索: [_____________] [🔍]                       │
├──┬────────┬────────┬────────┬────────┬──────────┤
│ID│ 用户名 │ 邮箱   │ 角色   │ 状态   │ 操作     │
├──┼────────┼────────┼────────┼────────┼──────────┤
│1 │ admin  │ a@x.com│ ADMIN  │ 活跃   │ [查看]   │
│2 │zhangsan│ z@x.com│ USER   │ 活跃   │ [查看]   │
│3 │ lisi   │ l@x.com│ USER   │ 正常   │ [查看]   │
│4 │wangwu  │ w@x.com│ USER   │ 禁用   │ [启用]   │
└──┴────────┴────────┴────────┴────────┴──────────┘
```

**操作功能**:
- 查看用户详细信息
- 查看用户的衣物、穿搭、旅行数据
- 禁用/启用用户账号
- 删除用户（慎用）

### 4.4 活动日志

```
日志查看页面:
┌──────────────────────────────────────────────┐
│ 活动日志                                     │
├──────────────────────────────────────────────┤
│ 时间范围: [今天▼]  用户: [全部▼]            │
│ 操作类型: [全部▼]                            │
├──────┬──────────┬──────────┬────────────────┤
│ 时间 │ 用户     │ 操作类型 │ 描述           │
├──────┼──────────┼──────────┼────────────────┤
│10:30 │ zhangsan │ 登录     │ 用户登录系统   │
│10:32 │ zhangsan │ 添加衣物 │ 添加白色T恤    │
│10:35 │ lisi     │ 创建穿搭 │ 创建夏日约会装 │
│10:40 │ admin    │ 查看统计 │ 访问管理后台   │
└──────┴──────────┴──────────┴────────────────┘
```

---

## 5. 常见问题FAQ

### 5.1 账号相关

**Q1: 忘记密码怎么办？**
A: 目前系统不支持自助找回密码。请联系管理员重置密码。

**Q2: 可以修改用户名吗？**
A: 用户名注册后不可修改，但可以修改昵称、邮箱等其他信息。

**Q3: 如何退出登录？**
A: 点击右上角头像，选择"退出登录"即可。

### 5.2 衣物管理

**Q4: 支持上传多张图片吗？**
A: 目前每件衣物支持上传1张图片。多图功能规划中。

**Q5: 图片上传失败怎么办？**
A: 
- 检查图片格式（支持JPG、PNG）
- 确保图片大小不超过5MB
- 检查网络连接
- 清除浏览器缓存重试

**Q6: 删除衣物后可以恢复吗？**
A: 删除操作不可恢复，请谨慎操作。

### 5.3 系统使用

**Q7: 支持手机端使用吗？**
A: 系统主要面向PC浏览器设计，手机端可访问但体验可能不佳。

**Q8: 数据会不会丢失？**
A: 系统定期备份数据，正常使用不会丢失。建议定期导出重要数据。

**Q9: 系统支持多语言吗？**
A: 目前仅支持中文，多语言功能规划中。

### 5.4 性能问题

**Q10: 页面加载很慢？**
A:
- 检查网络速度
- 清除浏览器缓存
- 尝试其他浏览器
- 联系管理员检查服务器状态

**Q11: 图片显示不出来？**
A:
- 检查图片URL是否正确
- 确认uploads目录权限
- 检查Nginx配置
- 查看浏览器控制台错误

---

## 6. 故障排查

### 6.1 Docker部署问题

#### 问题1: 容器启动失败

```bash
# 查看容器状态
docker compose ps

# 查看容器日志
docker compose logs mysql
docker compose logs backend
docker compose logs frontend

# 常见原因:
# 1. 端口被占用
sudo lsof -i :3306
sudo lsof -i :8080
sudo lsof -i :80

# 2. 权限问题
sudo chown -R $(whoami) ./backend/uploads
chmod -R 755 ./backend/uploads

# 3. 配置错误
# 检查docker-compose.yml配置
# 检查环境变量.env
```

#### 问题2: MySQL连接失败

```bash
# 进入MySQL容器
docker compose exec mysql bash

# 测试MySQL
mysql -u wardrobe_user -p
SHOW DATABASES;

# 检查权限
SHOW GRANTS FOR 'wardrobe_user'@'%';

# 重建MySQL容器
docker compose down mysql
docker compose up -d mysql
```

#### 问题3: 后端服务无响应

```bash
# 查看后端日志
docker compose logs -f backend

# 进入后端容器
docker compose exec backend bash
curl http://localhost:8080/api/health

# 重启后端
docker compose restart backend

# 检查数据库连接
# 查看application.yml配置
```

### 6.2 手动部署问题

#### 问题1: 后端启动失败

```bash
# 查看日志
tail -f backend.log

# 常见错误:
# 1. 数据库连接失败
# 检查MySQL是否启动
systemctl status mysql

# 测试连接
mysql -h localhost -u wardrobe_user -p

# 2. 端口被占用
netstat -tlnp | grep 8080
# 关闭占用进程或修改端口

# 3. Java版本不匹配
java -version
# 确保使用Java 11+
```

#### 问题2: 前端访问404

```bash
# 检查Nginx配置
nginx -t

# 重启Nginx
systemctl restart nginx

# 检查前端构建产物
ls -la /var/www/html

# 检查Nginx日志
tail -f /var/log/nginx/error.log
```

#### 问题3: 图片无法访问

```bash
# 检查uploads目录
ls -la backend/uploads

# 检查权限
chmod -R 755 backend/uploads

# 检查Nginx配置中的路径
cat /etc/nginx/sites-available/wardrobe | grep uploads

# 测试访问
curl -I http://localhost/uploads/test.jpg
```

### 6.3 性能问题排查

```bash
# 检查系统资源
top
df -h
free -m

# 检查数据库性能
mysql -u root -p
SHOW PROCESSLIST;
SHOW STATUS LIKE 'Threads_connected';

# 检查慢查询
SHOW VARIABLES LIKE 'slow_query_log';
SELECT * FROM mysql.slow_log LIMIT 10;

# 检查日志大小
du -sh backend/logs
# 定期清理旧日志
```

### 6.4 日志查看命令

```bash
# Docker环境
docker compose logs -f                  # 所有服务
docker compose logs -f backend          # 后端日志
docker compose logs -f mysql --tail=100 # MySQL最后100行

# 手动部署
tail -f backend.log                     # 后端日志
tail -f /var/log/nginx/access.log       # Nginx访问日志
tail -f /var/log/nginx/error.log        # Nginx错误日志
tail -f /var/log/mysql/error.log        # MySQL错误日志
```

---

## 7. 附录

### 7.1 快捷键列表

| 快捷键 | 功能 |
|--------|------|
| Ctrl + / | 快速搜索 |
| Ctrl + N | 新建衣物 |
| Ctrl + S | 保存当前表单 |
| Esc | 关闭对话框 |

### 7.2 API测试示例

使用curl测试API:

```bash
# 1. 用户注册
curl -X POST http://localhost:8080/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "password123",
    "email": "test@example.com"
  }'

# 2. 用户登录
TOKEN=$(curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "password123"}' \
  | jq -r '.token')

# 3. 获取衣物列表
curl -X GET http://localhost:8080/api/clothing \
  -H "Authorization: Bearer $TOKEN"

# 4. 添加衣物
curl -X POST http://localhost:8080/api/clothing \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "白色T恤",
    "category": "TOP",
    "color": "白色",
    "brand": "优衣库",
    "price": 99.00,
    "size": "M",
    "season": "SUMMER"
  }'
```

### 7.3 数据库维护

```sql
-- 查看数据库大小
SELECT 
  table_schema AS 'Database',
  ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Size (MB)'
FROM information_schema.TABLES
WHERE table_schema = 'wardrobe'
GROUP BY table_schema;

-- 查看表大小
SELECT 
  table_name AS 'Table',
  ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Size (MB)'
FROM information_schema.TABLES
WHERE table_schema = 'wardrobe'
ORDER BY (data_length + index_length) DESC;

-- 优化表
OPTIMIZE TABLE clothing;
OPTIMIZE TABLE outfits;
OPTIMIZE TABLE travel_plans;

-- 备份数据库
-- mysqldump -u wardrobe_user -p wardrobe > backup_$(date +%Y%m%d).sql

-- 恢复数据库
-- mysql -u wardrobe_user -p wardrobe < backup_20251215.sql
```

### 7.4 系统监控

**健康检查脚本** `health-check.sh`:

```bash
#!/bin/bash
# 系统健康检查脚本

echo "========================================="
echo "  智衣橱系统健康检查"
echo "========================================="
echo ""

# 检查前端
echo -n "前端服务: "
if curl -f http://localhost/ >/dev/null 2>&1; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

# 检查后端
echo -n "后端API: "
if curl -f http://localhost:8080/api/health >/dev/null 2>&1; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

# 检查MySQL
echo -n "MySQL数据库: "
if docker compose exec -T mysql mysqladmin ping -h localhost --silent 2>/dev/null; then
    echo "✅ 正常"
else
    echo "❌ 异常"
fi

echo ""
echo "容器状态:"
docker compose ps
```

### 7.5 联系支持

如遇无法解决的问题，请联系技术支持:

- 📧 Email: support@smartwardrobe.com (示例)
- 📱 Phone: 400-xxx-xxxx (示例)
- 💬 在线客服: 周一至周五 9:00-18:00
- 🐛 Bug报告: https://github.com/MrWeilaity/Wardrobe/issues

---

**文档版本**: V2.0  
**最后更新**: 2025-12-15  
**维护团队**: 智衣橱技术团队  

---

*本手册会持续更新，请关注最新版本。感谢您使用智衣橱系统！*
